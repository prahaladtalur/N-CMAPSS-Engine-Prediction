# N-CMAPSS Engine Prediction - Dependencies
# 
# NOTE: This project uses pyproject.toml as the primary dependency source.
# This file is kept for compatibility but should be synchronized with pyproject.toml.
# 
# For new installations, use: uv sync (recommended) or pip install -e .

rul-datasets>=0.17.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.12.0
scikit-learn>=1.0.0
tensorflow>=2.0.0
h5py>=3.0.0
tqdm>=4.62.0
wandb>=0.15.0
python-dotenv>=1.0.0
jupyter>=1.0.0
urllib3<2.0.0
